# 2026-09-15 v0.1.234 发布与生产验收

本文时间除特别说明外均为北京时间（UTC+8）。

## 授权、范围与版本

- 用户明确授权：推送到远端，通过 CI 后更新 tag 自动部署。
- 修复提交：`66c9ee8a03ee589317ccd3e24ccf6bc67a48eb23`；发布候选：`cfd9afa871def9ebd457bb95fb4c2e8b4b02b34f`。相对推送前远端 main 仅 3 个文件、129 行新增：流收尾逻辑、回归测试及二开历史记录。
- 修复 Anthropic API Key 透传收到并完整转发终止事件后仍等待 EOF 的误报超时。不补造 `message_stop`，真正缺失终止事件的断流、超时仍报错；最终用量、客户端断连处理及 `[DONE]` 兼容保留。
- 本地复跑定向 unit 与 race 测试成功，12 项远端部署门禁测试成功。没有新增数据库迁移、修改账号或计费规则、调整生产超时与资源配置。
- 15:19 推送 main；固定 SHA 的 CI、安全扫描全部成功后，于 15:33 推送新的 annotated tag `v0.1.234`。标签对象 `9f2913970edec25ec7e2aaca86a7db1fc63d1b5c` 指向候选，没有覆盖旧标签或推送其他标签。GitHub SSH 短暂断连时先核对远端标签不存在再重试。

## CI 与自动部署

| 阶段 | Actions 运行 ID | 结果 |
| --- | --- | --- |
| main CI | `34941090320` | 成功；后端单元/集成、前端、静态检查及 macOS shell 通过 |
| main 安全扫描 | `34941090352` | 成功 |
| tag CI | `34942303928` | 成功；全部任务通过 |
| tag 安全扫描 | `34942303892` | 成功 |
| Release | `34942303933` | 成功；版本产物、前端构建、正式发布、版本回写、实际生产部署均成功 |

- 正式 Release 页面发布时间为 15:47:51。实际 `Deploy on remote host` 步骤于 15:48:10 开始、15:48:34 成功，部署任务于 15:48:36 完成；不是跳过部署的绿色状态。
- 仅自动部署至服务器1 `216.152.153.86:22748` 及指定活动 Compose，没有访问服务器2或旧生产机。手工 SSH 均为只读验收，保留严格主机密钥检查，没有上传或改写生产 Compose。
- 工作流自动回写 `backend/cmd/server/VERSION` 为 `0.1.234`，提交 `742cdd32c1f7cdf52503bf01c994948b1784a95d` 仅修改版本文件；本地通过 fetch 和 `merge --ff-only` 同步。

## 生产验收

- 15:49:42 只读前后快照比较通过；应用于 15:48:28 启动，运行镜像 `saviour2411/sub2api:0.1.234`，OCI revision 为 `cfd9afa871def9ebd457bb95fb4c2e8b4b02b34f`，与通过 CI 的标签候选一致。
- 镜像 ID：`sha256:fad448fb38c3dbe55ce5aade30f17bc1b1273a8fe38cf1d4d6453eb827afe7eb`；仓库摘要：`sha256:c4f0246de08231cc0f13829c8521b156a7480aaabfef11eb183f6981003aef9d`。
- 应用、PostgreSQL、Redis 均 running/healthy、重启计数为 0、无 OOMKilled。数据库和 Redis 的容器 ID、镜像、启动时间均未改变，没有重建。
- 两份 Compose、持久化配置、剔除镜像/标签后的 `.env` 内容摘要均与部署前一致；三容器的数据挂载、端口、内存限制、共享内存、日志轮转及快照中的白名单运行参数均未改变。应用仍仅绑定 `127.0.0.1:18080`。
- 双 Compose SHA256：`cd01abf4f418a143a1bfca4be568b5a1afd6ebce8fe0a8301b06caf52768f3b6`；持久化配置 SHA256：`8f01b13b6a126e82f7a6e3d6c9eab9fa3ebe8fcaca23a01b7de49783fdf41ed4`。
- 本机 `/health` 返回 HTTP 200 与 `{"status":"ok"}`；15:52:34 从生产机访问公网 API 域名 `/health` 返回 HTTP 200 与 ok。本地开发机出口访问同一域名返回 403，未将其忽略或宣称所有外部网络路径已验证。

## 关停观察与未验证边界

- 发布前已提示现有 HTTP 优雅退出仅等待 5 秒，长请求可能中断，不承诺零中断。
- 15:36:23 开始只读跟随旧容器日志；15:48:23.292 记录 `Server forced to shutdown`，15:48:23.296 记录 `usage_record.task_dropped`，15:48:26.311 记录 `All cleanup steps completed`。日志观察进程正常结束。
- 持久化日志确认丢弃原因 `stopped`，当时 `running_workers=1`、`waiting_tasks=2`、`dropped_queue_full=0`、`dropped_pool_stopped=1`、`sync_fallback_tasks=0`。这是任务池停止后的用量任务提交丢弃，不是队列满。
- `usage_record.task_dropped` 日志有限频，单条告警不能证明精确受影响请求总数，也不能直接确认具体账单结果。本次没有进一步关联用户账单，没有补写、重算或回滚；不宣称零中断或账单完整。
- 未对真实第三方模型执行付费请求，未做峰值压测、完整账单核对或全包 race。修复行为由定向测试及 CI 验证，生产验收覆盖运行版本、配置和健康，不等同真实模型端到端验证。
- GitHub 未认证 API 后期达到读取配额，Release 成功及实际部署步骤已在限流前获取；正式发布页另以 HTTPS 读取核验。没有把状态读取受限当作部署失败或跳过生产核验。
- 脱敏证据与辅助脚本保留在项目内 `.analysis_tmp/release-v0.1.234-*`，不纳入提交；未记录私钥、令牌、请求正文或用户账单明细。
