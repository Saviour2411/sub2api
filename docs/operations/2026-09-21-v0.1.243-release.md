# v0.1.243 可空字段迁移与蓝绿发布

> 时间均为北京时间（UTC+8）。22:24:41 旧槽退役后恢复 stable，22:25:37 最终生产验收通过，Release 全部成功。旧槽发生到期强退，不代表旧请求无中断或用量完整。

## 授权与版本

- 用户要求更新标签并经 GitHub Actions 自动蓝绿部署，在了解共享数据库、锁等待和回退边界后，进一步批准旧版兼容的字段扩展门禁。
- 发布提交：`6154c64ffc83f180f7e8f118ccbd5182b8cb0f52`，已通过候选 CI 后快进合入 `main`；包含前一轮已同步的46个上游提交及本次 `CUST-OPS-007` 迁移保护。
- 21:14:38 推送新附注标签 `v0.1.243`，标签对象 `3c3d833594a8205d8a7e946fe14c04b3419b21fe`；没有覆盖历史标签。版本文件预先更新为 `0.1.243`，Actions 回写步骤无需新增版本提交。
- 发布前活动版本 `0.1.242`，提交 `ee1c56f75a38c8678ed338a4a1fecad51b08593c`，green 槽状态 `stable`、`pending=null`。

## CI 与发布

| 范围 | 工作流 | Run | 结果 |
| --- | --- | --- | --- |
| 候选分支 | CI | 35602993531 | 8项成功 |
| 候选分支 | Security Scan | 35602993293 | 2项成功 |
| 主线 | CI | 35604246636 | 8项成功 |
| 主线 | Security Scan | 35604246686 | 2项成功 |
| 标签 | CI | 35604349965 | 8项成功 |
| 标签 | Security Scan | 35604350148 | 2项成功 |
| 标签 | Release | 35604350151 | 10项作业及实际蓝绿部署步骤全部成功 |

- 21:22:50 发布公开 Release，`draft=false`、`prerelease=false`。Linux/macOS amd64/arm64、Windows amd64 五目标构建全部通过；GitHub 附件 digest 与 `checksums.txt` 五项逐一一致，校验和文件自身 SHA256 亦核对成功。本轮未下载并运行五个平台二进制。
- 生产固定镜像：`saviour2411/sub2api@sha256:ce498d5c6f434bcb869d1d1bfc12ed48b9c980f36eb83e2384bf97e6d9c2ebbf`，运行镜像 revision 与上述发布提交一致。
- 21:23:17 Actions 完成固定 CI、镜像摘要和迁移兼容证据绑定，21:23:19 进入服务器执行器。人工服务器操作仅为只读验收，未手动执行 DDL、切流、重启或退役。
- 22:24:48 生产部署作业成功；已检查 `Blue-green deploy and observe natural drain` 实际执行成功，不把跳过部署的绿色工作流当作发布完成。

## 迁移保护与结果

- 普通自动路径不再一概拒绝新增可空字段，但必须有绑定当前旧提交、SQL 摘要和旧代码摘要的兼容审查。仅支持单条新增可空内置类型字段；默认值、约束、回填、历史 SQL 改写、删除、改名、Ent schema 和未知变化继续拒绝。
- 当前原始迁移 `238b_content_moderation_engine_meta.sql` 保持不变；摘要 `420af91eec85bdb60474640612a0f4d069db6ddfb21e6ed702b89713f3599287`。旧版审核日志使用明确列名读写；规则与恢复边界见 `blue-green-expand-migrations.md`。
- 候选迁移入口先核对实际完整待执行清单，再以 NOWAIT 表锁和短事务限制执行。扩展候选启动时关闭自动重启，依赖和合成流成功后才恢复 `on-failure`，失败不自动重试或切流。
- 本地证据测试6项、状态机51项、预检3项、Go定向单元和隔离PostgreSQL集成测试通过，golangci-lint 0 issues。NTFS socket限制在仓库内POSIX临时目录重跑原断言后通过，未关闭测试。
- 20:58 生产只读预检：实际待执行项唯一为238b，目标是无分区/继承的普通表，采样时无该表锁或长事务。备份记录最近完成于同日12:12:53，约3.67GB；本轮没有下载或恢复演练，不把备份记录状态当作可恢复性证明。
- 21:25:51 验收：迁移账本记录摘要匹配，`engine_meta` 实际为 `jsonb`、允许NULL、无默认值；活动容器的策略只包含该文件，重启策略已恢复 `on-failure`。迁移前后采样表占用均为4415488字节，不将此采样作为全负载零延迟证明。
- 22:25 最终核验再次确认字段定义、迁移摘要和活动策略一致；全部历史账本条目逐项保持原摘要，仅新增获批238b这一项。实际发布归档的兼容旧提交、SQL摘要和容器策略一致。

## 切流与保护项

- 21:23:32 切流至 blue/18080，双入口确认约1.156秒。活动实例 `i4a2c0e2d6dd14fb688b7c0ba717fc290`。
- 21:26:50 本机及两个HTTPS入口验收通过，API/direct 均返回200并命中新实例。21:28公网 direct 入口独立检查也命中相同实例，公开版本为 `0.1.243`。
- 两个活动Compose字节一致；发布前后Compose、`data/config.yaml` 摘要不变。通用响应头超时3600秒、用量任务超时5秒、已批准CPU/内存/连接池/worker参数和HTTP upstream策略均保留。
- PostgreSQL与Redis容器身份、启动时间、重启计数不变，原应用/数据库/Redis bind mount保持不变。没有重建数据服务、切换命名卷、修改生产`.env`或访问服务器2/旧生产机。
- 21:53 中段及22:25:37最终只读验收全部通过；与发布前、切流后快照逐项比较确认新blue容器身份、启动时间、重启计数和实例ID不变，Compose、持久配置、资源参数和数据挂载保持不变。PostgreSQL既有父目录匿名卷不承载活动数据库数据，未改动其原始bind mount。

## 排空与退役

- 原有一小时观察及到期强制退役策略保持不变，与数据库字段兼容审批分开记录。观察截止约22:23:32。
- 21:26:50 旧green仍有HTTP 75、SSE 63、会话租约2943；21:38:18降至HTTP 39、SSE 26、租约2445。采样时待入队/执行中用量均为0；这些瞬时计数不能证明全量用量无遗漏。
- 22:23:32 到期触发强制退役。绑定本次发布SHA的关停凭据记录旧green为HTTP 10、SSE 4、会话租约495，用量pending/running/producers均为0；这与其他时刻的周期采样不同，不混用计数。
- 60秒关停宽限后，22:24:37旧容器停止，退出码137、`oom_killed=false`、`logs_verified=true`、`clean_exit=false`、`usage_loss_unknown=true`。虽然日志中用量丢弃和强制关停标记计数均为0，仍不能证明无用量遗漏；未补写账单或擅自改变退役策略。
- 清理旧实例归属476项后，22:24:41恢复`stable`、`active=blue`、`pending=null`、`last_error=null`，旧green已移除。强制结果的旧/新实例、容器ID和发布SHA均与本轮基线一致；未将上次发布的残留退役记录误认为本次结果。
- 本次Actions归档中API/direct健康探针各3508次，合计7016次，错误列表均为空。健康端点采样不代表所有客户请求无错误、旧长请求无中断、账单完整或峰值负载安全。

## 归档校验

- 已下载本次`blue-green-deployment-35604350151-1`归档，artifact ID `10644412631`；ZIP SHA256 `3d85e546b7a862518bc684281d5f73f768c3432e5a9468954e6b3874be11c91e`，与GitHub记录一致。
- `release.json`绑定本次提交、固定镜像摘要、成功CI和安全扫描、旧提交`ee1c56f75a38c8678ed338a4a1fecad51b08593c`、`migration_class=expand`及唯一238b策略；所用CI证据为`35602993531`，安全扫描为`35604350148`，均对应同一固定提交。
- `result.json`与最终生产实例和退役状态一致，`observation.json`保存上述连续健康探针结果。本轮未执行真实付费模型/支付回调、完整账单核对、恢复演练或峰值压力测试，不把局部验收扩展为这些链路全部通过。

## 证据位置

本地证据位于 `output/release-20260921-v0.1.243/`：候选/主线/标签CI、`tag.json`、`published-release.json`、`asset-audit.json`、`migration-before.json`、`migration-after.json`、`migration-final.json`、`production-before.json`、`production-after-switch.json`、`production-midpoint.json`、`production-final.json`、`production-final-comparison.json`、`public-health.json`、`observations.jsonl`、`release-latest.json`、`deployment-evidence.zip`及解压内容、`deployment-artifact-audit.json`。目录不纳入Git，不保存认证凭据或用户明文资料。
