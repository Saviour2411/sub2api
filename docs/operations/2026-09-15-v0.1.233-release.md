# v0.1.233 发布与生产验收

本文时间均为北京时间。关联能力：`CUST-OPS-001`、`CUST-OPS-003`、`CUST-OPS-005`；本记录不修改这些能力的行为或默认值。

## 授权与发布范围

- 用户在本地同步完成后追加授权：任务未完成则继续，已完成则提交代码，通过 CI 后更新 tag 自动部署。
- 固定上游范围仍为 `98d86915becae9fe9491a91ffc6defd5235c8d2b..bdb42e22f81fcb633ff0a060961211dd2bcb515b`，141 个提交全部 Applied，其中 16 个包含本地覆盖；没有扩大同步范围。
- 合并代码提交：`6454c4f68754a0e79ae6cce549ac616999d0ab57`；同步记录与发布候选提交：`148cd9afab8b0bf4b8dbc3ba00c8b4cd8b96cce5`。候选相对同步前共 366 文件变化，57 项定制能力保留。
- 先推送 `main`，等待该候选的 CI 和安全扫描全部通过，再于 2026-09-15 03:54:48 创建并推送新的 annotated tag `v0.1.233`。标签对象为 `f6d9b40d68e3cb6a12c5fc8aa22fac1ffb4aeae2`，指向上述候选；没有覆盖旧标签或推送其他标签。
- 发布工作流自动回写 `backend/cmd/server/VERSION` 为 `0.1.233`，提交为 `2b9db482350d72ce37c1f4f76e62d8c74df06a52`，仅该版本文件变化；本地已通过显式 fetch 和 `merge --ff-only` 接收。

## CI 与自动部署

所有运行均属于 `Saviour2411/sub2api`，候选 SHA 相同。

| 阶段 | Actions 运行 ID | 结果 |
| --- | --- | --- |
| main CI | `34888272480` | 成功；Go 单元和集成、前端、golangci-lint、macOS shell 全部通过 |
| main 安全扫描 | `34888272483` | 成功；后端及前端安全检查通过 |
| tag CI | `34889754413` | 成功；全部任务通过 |
| tag 安全扫描 | `34889754535` | 成功；全部任务通过 |
| Release | `34889754841` | 成功；版本产物、双前端、正式发布、版本回写、生产部署全部通过 |

- Release 于 04:07:16 发布，包含校验文件和 Darwin/Linux 双架构、Windows amd64 共 5 份压缩产物。
- 实际 `Deploy on remote host` 步骤于 04:07:36 开始、04:07:57 成功，不是跳过部署的绿色工作流；部署任务于 04:08:00 完成。
- 自动部署只使用新生产机 `216.152.153.86:22748` 和 `/root/proj/sub2api/deploy/docker-compose.yml`。没有连接旧机或服务器2，没有上传或改写生产 Compose，没有手工重跑生产迁移或回退数据库。
- 部署前只读 SSH 使用迁移时保存的 ED25519 主机记录进行严格校验，保留主机密钥校验；没有安装生产依赖。脚本及脱敏报告放在本地仓库忽略目录，远端检查从标准输入执行。

## 生产验收

- 04:10:48 只读验收通过。运行镜像为 `saviour2411/sub2api:0.1.233`；OCI revision 为 `148cd9afab8b0bf4b8dbc3ba00c8b4cd8b96cce5`，与通过 CI 的标签候选完全一致。
- 镜像 ID：`sha256:42a926caad16a820f2447d92afb2f8a298e966aaa5347dff7c8e9dbd16eb61c1`；仓库摘要：`sha256:85a3cb1820a8aeb096c0317709a9c460dd191276a164752b8516fd29ca29ffd5`。
- 新应用于 04:07:52 启动，应用、PostgreSQL、Redis 均为 running/healthy，重启计数为 0，无 OOMKilled。数据库和 Redis 的容器 ID、启动时间、挂载及资源参数与部署前完全一致，没有重建。
- 三个实际数据目录继续使用原 bind mount：`data -> /app/data`、`postgres_data -> /var/lib/postgresql/data`、`redis_data -> /data`。PostgreSQL 父目录 `/var/lib/postgresql` 的既有匿名卷未变，实际 `data_directory` 仍在指定 bind mount 内。
- 双 Compose 字节一致，部署前后 SHA256 均为 `cd01abf4f418a143a1bfca4be568b5a1afd6ebce8fe0a8301b06caf52768f3b6`。
- 持久配置 `data/config.yaml` 的 SHA256 均为 `8f01b13b6a126e82f7a6e3d6c9eab9fa3ebe8fcaca23a01b7de49783fdf41ed4`；结构化解析确认 `gateway.response_header_timeout=3600`，不存在环境变量覆盖。
- 应用仅绑定 `127.0.0.1:18080`。三个 HTTP upstream 安全开关继续为 `false/true/true`。
- 应用保留 `GOMAXPROCS=256`、`GOMEMLIMIT=216181080064B`、数据库连接池 `512/32`、HTTP 连接参数 `2048/2048/256`、用量 worker `64`、队列 `16384`、任务超时 `5` 秒、自动扩缩关闭。
- PostgreSQL 保留 max_connections `600`、shared_buffers `64426MB`、effective_cache_size `128853MB`、maintenance_work_mem `128MB`、并行 `4/2`、共享内存 `2g`；三容器日志轮转继续为 `100m` 和 `3` 份。没有调整 BBR/CAKE 或其他系统配置。
- 本机 `/health` 返回 ok；04:11:41 公网 API `/health` 返回 HTTP 200 和 ok。

相对上一生产标签的 3 份新增 SQL 均由新应用启动自动执行，时间为 04:07:52。按迁移运行器的 `strings.TrimSpace` 后 SHA256 算法，与 tag 中的 SQL 逐一匹配：

| 迁移 | 校验和 |
| --- | --- |
| `237_user_customizations.sql` | `373c636f557e6336146ee3708138d74064c0ce5c9968eceaf68be42b5f1bb254` |
| `238_opencode_go_platform.sql` | `6f987e251519bd3759e60da44620a5d777494cceb333b6ce394aa0ea536ef5a2` |
| `238_purge_unlimited_user_platform_quotas.sql` | `052756d1b1ac951f002034bf0434f8a6ea943ec0a2a34541ca4374ba6278cce7` |

## 关停观察与边界

- 03:53 预检采样为 15 个普通请求槽位，最早 128 秒；04:00 再次采样为 23 个，最早 90 秒，Live 槽位为 0。槽位是 Redis 聚合信号，可能含过期项或未覆盖全部连接，不能等同精确在飞请求数。
- 发布前已说明应用仅 5 秒 HTTP 优雅退出等待，未承诺无中断。03:59:06 至 04:07:51 的旧容器只读日志观察发现 `Server forced to shutdown`，随后出现 `All cleanup steps completed`；未观察到清理超时、清理失败或 `usage_record.task_dropped` 告警。
- 因为确实发生 HTTP 强制关停，不能将本次升级描述为零中断；没有丢弃告警也不能证明全部账单完整或所有请求无损。没有对历史账单执行补写或重算。
- 发布过程中一次只读采样碰到新容器健康状态尚未就绪，被校验脚本拒绝；待实际部署成功后重测通过。验收脚本首次将迁移按原始字节计算哈希，与运行器的去除首尾空白规则不同；修正验收脚本算法后 3 份全部一致，没有改生产数据或迁移文件。
- 真实第三方模型、支付/回调、短信邮件、完整浏览器业务端到端、全包 race、峰值负载和账单完整性仍不属于完整验证范围。既有前端依赖例外未新增或放宽；Go 软内存与 PostgreSQL 缓存叠加的极端 OOM 风险仍保留。
- 原始秘密不进入报告或提交。本地脱敏证据位于 `output/release-20260915-bdb42e22f/`，包括固定 SHA 的 CI/Release 结果、版本/标签、前后配置摘要、迁移校验和及关停聚合观察；不纳入 Git。
