# v0.1.242 定制版蓝绿发布与生产验收

> 时间均为北京时间（UTC+8）。Release 全部任务成功，04:01:31 旧槽回收后恢复 `stable`、`pending=null`。旧实例为到期强制退役，退出 137 且非 OOM，不能视为无损退役或用量完整性证明。

## 授权与固定版本

- 用户明确要求提交并推送本次代码、合入主线，在 CI 通过后创建发布标签，通过 GitHub Actions 自动蓝绿部署。
- 功能提交：`ee1c56f75a38c8678ed338a4a1fecad51b08593c`，已推送 `main`。新增附注标签 `v0.1.242` 固定指向该提交，没有覆盖旧标签。
- 本次移除网页自更新、回滚、重启入口及五个服务端接口，只读版本直接返回当前构建版本；用户定制搜索增加邮箱包含匹配，管理员身份显示增加邮箱兜底。
- 无数据库迁移、Ent schema、发布工作流或部署脚本变化；保留共享 GitHub 客户端、首次安装重启、现有一小时观察及到期强制退役策略。不修改生产资源、开关、配置或数据挂载。
- 发布前版本为恢复后的 `0.1.241`，活动槽 `blue/18080`，状态 `stable`、`pending=null`。

## CI 与发布证据

| 范围 | 工作流 | Run | 结果 |
| --- | --- | --- | --- |
| 主线固定功能提交 | CI | [35461557667](https://github.com/Saviour2411/sub2api/actions/runs/35461557667) | 7 项全部成功 |
| 主线固定功能提交 | Security Scan | [35461557693](https://github.com/Saviour2411/sub2api/actions/runs/35461557693) | 2 项全部成功 |
| `v0.1.242` | CI | [35462200010](https://github.com/Saviour2411/sub2api/actions/runs/35462200010) | 7 项全部成功 |
| `v0.1.242` | Security Scan | [35462200045](https://github.com/Saviour2411/sub2api/actions/runs/35462200045) | 2 项全部成功 |
| `v0.1.242` | Release | [35462200005](https://github.com/Saviour2411/sub2api/actions/runs/35462200005) | 5 项全部成功，生产部署实际执行 |

- 02:44:07 完成主线门禁核验后才推送标签；02:59:45 GitHub Release 公开发布，`draft=false`、`prerelease=false`。
- 发布包含校验和及 Linux/macOS amd64/arm64、Windows amd64 附件。生产使用固定摘要：`saviour2411/sub2api@sha256:b9f3d64ad289478a266274aa93710944734e77a7a97f4b1431d95f1eee724a4d`。
- Actions 自动提交 `a86c4b218` 将 `backend/cmd/server/VERSION` 回写为 `0.1.242`；本地已快进同步，没有移动发布标签。
- 部署 artifact `blue-green-deployment-35462200005-1`（ID `10591750837`）已下载核验：`release.json` 绑定固定 SHA、镜像摘要、上述标签 CI/安全扫描和 `migration_class=none`；兼容基线为 `eed20825008d86dda7828c3f6100b29e4054cbd8`，`result.json` 与生产最终状态一致。

## 生产切流与功能检查

- 03:00:22 完成切流，双入口确认耗时约 1.176 秒。活动槽为 `green/18082`，仅绑定 `127.0.0.1`。
- 新实例：`i10cdf020402d4217b5c3347fd0955737`；容器：`23447c65deb4fcf836744ef58a12eecbaa014fa7dcab0400522c4603b83741b1`。
- 03:02:26 只读验收全部通过：本机 `/health` 正常，API/direct 两个 HTTPS 入口的 `/readyz` 均返回 200，并命中上述 green 实例。公共设置及管理员只读版本接口均返回 `0.1.242`，未认证访问管理员版本仍为 401。
- 03:23:33 从本机访问公网 direct 入口也返回 200、命中同一 green 实例并显示 `0.1.242`；03:29:15 中段复核及 04:03:14 退役后最终复验通过，实例身份和启动时间未变。04:03:52 容器清单仅保留 green、PostgreSQL、Redis，旧 blue 已移除。
- 已移除接口实测：GET `check-updates`、`rollback-versions`，POST `update`、`rollback`、`restart` 均返回 404。探针仅在固定新版镜像修订核验通过后发送，没有执行旧版更新或重启。
- 使用服务器既有管理员 API Key，在远端进程内存中调用只读列表接口：`box`、`BOX`、` box `、`1054` 均返回 200、总数 1，结果包含 ID 1054，用户名为空且管理员响应包含邮箱。未回传凭据、明文邮箱或余额，不生成新凭据，不修改用户资料、授信或查询链接。
- 桌面 1440×1000、移动端 390×844 的版本只读、搜索、编辑/确认身份、长邮箱换行及公开页邮箱隔离已在实现阶段通过 Playwright 验证；生产本次验证为实际只读 API 与入口检查，不把本地夹具截图描述成生产页面截图。

## 数据与配置保护

03:02:26 切流后、03:29:15 观察中及 04:03:14 退役后快照与 02:38:25 基线比较，以下项目全部一致：

- PostgreSQL 容器 `c173e2ee1480fd3c25d1627a427cc635bb3273ae0ecc3fd6a7935978548e97cb`、Redis 容器 `7372590688e9aa0a31f554364e18365f4fea6ffba686e877661e31b18dc674b6` 的身份、启动时间和重启次数未变，均未重建或重启。
- 应用 `deploy/data -> /app/data`、PostgreSQL `deploy/postgres_data -> /var/lib/postgresql/data`、Redis `deploy/redis_data -> /data` 均保持原 bind mount。PostgreSQL 的既有父目录匿名卷未被误改为业务数据卷。
- 双 Compose 完全一致，SHA256 均为 `cd01abf4f418a143a1bfca4be568b5a1afd6ebce8fe0a8301b06caf52768f3b6`。
- `data/config.yaml` SHA256 为 `8f01b13b6a126e82f7a6e3d6c9eab9fa3ebe8fcaca23a01b7de49783fdf41ed4`，通用响应头超时保持 3600 秒；`.env` SHA256 为 `23953c721c1c4c74fd23739226e96066b2ea5b8307b1bcab9c1684074216fdb7`。
- `GOMAXPROCS=256`、`GOMEMLIMIT=216181080064B`、数据库池 512/32、worker 64、队列 16384、用量任务超时 5 秒、关闭自动扩缩、HTTP upstream 策略均保持原值。PostgreSQL 最大连接 600、共享缓存 64426MB、有效缓存 128853MB、维护内存 128MB、并行参数 4/2 未变。
- 安全重试与提前 JSON 保活开关保持发布前已由管理员启用的 `true/true`；本次发布不自动开启或关闭开关。
- 新 green 的容器 ID、启动时间、重启次数、挂载与实例 ID 在整个观察窗口中保持一致。Docker inspect 的挂载数组返回顺序曾发生变化，按目标路径排序复核后内容完全相同；没有为消除比较差异而修改生产挂载。

## 退役与验收边界

- 切流后保留旧 blue 的现有请求与会话续接，观察窗口为一小时。03:01:54 旧实例仍有 HTTP 174、SSE 127、会话租约 2023，因此当时不能宣称已经排空。
- 04:00:22 到期触发强制退役，关停凭据记录旧实例 HTTP 17、SSE 10、会话租约 720，待入队、执行中用量和用量生产者均为 0。周期采样与关停凭据不是同一时刻，最终以绑定本次 SHA 的 `forced_retire_result.before` 为准。
- 关停宽限 60 秒；04:01:27 旧容器停止，退出码 137、`oom_killed=false`、`logs_verified=true`、`clean_exit=false`、`usage_loss_unknown=true`。日志中 `usage_drop_count=0` 和 `forced_shutdown_count=0` 不证明没有遗漏用量；本次未补写账单或擅自修改退役策略。
- 清理旧实例归属 701 项后，04:01:31 恢复 `stable`、`pending=null`、`last_error=null`。未重启新 green、PostgreSQL 或 Redis。
- 本次 Actions artifact 中 API/direct 健康探针各 3508 次，总计 7016 次，错误列表均为空。这只证明采样期间的健康端点可达，不代表所有客户请求无错误、旧长请求无中断或账单完整。
- 本轮人工服务器操作仅为只读检查，实际候选创建、切流、排空及退役由既有 GitHub Actions 执行；未访问服务器2或旧生产机。

## 本地证据

- `.cache/release-v0.1.242-main-checks-final.json`、`.cache/release-v0.1.242-v0.1.242-checks.json`：固定提交的工作流和任务结论。
- `.cache/release-v0.1.242-baseline.json`、`.cache/release-v0.1.242-post-switch.json`、`.cache/release-v0.1.242-post-switch-comparison.json`：发布前后只读审计及保护项比较。
- `.cache/release-v0.1.242-observation-*.json`：排空期间只读采样；`.cache/release-v0.1.242-published-assets.json`：公开发布附件及摘要。
- `.cache/release-v0.1.242-final-audit.json`、`.cache/release-v0.1.242-final-comparison.json`、`.cache/release-v0.1.242-final-preflight.json`：最终功能、保护项和旧槽移除核验；`.cache/release-v0.1.242-actions-final.json`、`.cache/release-v0.1.242-workflows-final.json`：最终 Actions 结论。
- `.cache/release-v0.1.242-deployment-evidence.zip`：原始部署 artifact，SHA256 `2d7cc941193a5cb8d8666e15e612d13915d84bacb3a3e7cbdfa88459a09fa33b`；解压内容和绑定核验分别保存在同名前缀目录及 `artifact-audit.json`。
- 上述本地证据不纳入 Git，不保存认证凭据、用户明文邮箱或余额；实际状态与 Actions artifact 共同作为发布记录来源。
