# 蓝绿兼容迁移门禁

2026-09-21 用户批准在旧版读写兼容的前提下支持这类数据库字段迁移。关联 `CUST-OPS-007`，沿用 `CUST-OPS-001` 的标签发布流程；这不是关闭迁移门禁。

## 普通可空字段扩展

- 仅新增 SQL 文件，文件中只有一条 `ALTER TABLE <表> ADD COLUMN IF NOT EXISTS <列> <类型>;`，可带独占一行的注释。
- 字段允许 NULL，无默认值、约束、回填、生成表达式或索引；类型限 PostgreSQL 内置 `jsonb/text/boolean/smallint/integer/bigint/uuid`。
- 必须在 `deploy/blue-green/expand-approvals.json` 留下针对活动旧提交的兼容审查，绑定 SQL 摘要、目标字段及旧版读写实现摘要。未来同类迁移仍需核验受影响旧版代码，不能仅凭 SQL 形状推断兼容。
- 历史 SQL 修改、删除、重命名、Ent schema 变化和其他未知文件继续拒绝；迁移目录内测试文件新增或修改不视为生产 DDL。
- 当前审批覆盖 `v0.1.242` 的 `ee1c56f75a38c8678ed338a4a1fecad51b08593c` 到 `238b_content_moderation_engine_meta.sql`：审核日志旧版明确指定列名，新字段不参与旧版读写，无余额或账单更新。

## 执行保护

1. 发布证据仍绑定固定 SHA、镜像摘要、完整 CI 和安全扫描；迁移策略随候选环境注入，不修改生产 `.env`、Compose 或业务参数。
2. 候选启动先核对真实 `schema_migrations` 和嵌入 SQL。任何额外待执行迁移、历史摘要不匹配或审批内容变化，都在 DDL 前失败。
3. 获批扩展仍使用既有迁移 advisory lock。每个文件独立事务，包含 DDL 和迁移记录；对目标表使用 `ACCESS EXCLUSIVE ... NOWAIT`，锁被占用立即失败，不排队等待旧请求。
4. 事务级 `lock_timeout=1s`、`statement_timeout=5s`，事务上下文最多 10 秒；设置不泄漏到业务连接池。不停止业务会话，不在数据库全局改超时。
5. 只接受既有普通表，拒绝分区、继承和类型表；完成后核对字段的实际类型、可空性、默认值和生成属性。`IF NOT EXISTS` 不能掩盖不兼容的同名字段。
6. 扩展候选启动期间 `restart=no`。迁移失败保留旧实例与当前流量，不自动重启候选；失败候选及 `preparing` 状态保留供核查。依赖与合成流通过后才恢复 `on-failure`，随后执行既有切流和排空流程。

## 风险与恢复

- 新旧实例共享数据库，迁移在切流前提交。新增可空列无需重写表，但获锁后仍会短暂持有排他锁；不能保证业务绝对零延迟。
- 失败事务回滚；已经成功提交的扩展保留。应用回退不执行反向删列，不恢复旧数据库覆盖新写入。
- 兼容审批不代替备份。生产发布前仍须核对实际待执行清单、备份情况、长事务、数据挂载、原有资源参数和入口健康。
- 单元测试覆盖不安全 SQL、错误基线/摘要、未知迁移和候选失败不切流；隔离 PostgreSQL 测试覆盖旧版写入、新字段写入、锁冲突立即失败、配置不泄漏、重复启动和字段漂移。生产负载下的零影响不在这些测试的证明范围内。
- 既有一小时观察及到期强制退役策略不变；它可能中断旧长请求，不能和迁移的结构兼容性混为一谈。

## 2026-09-24 专项239/240

用户在确认239旧计费键转换及240索引风险后批准专项兼容适配、验证、CI和标签自动蓝绿发布。此批准只覆盖以下两个原始SQL摘要，不改写上游迁移，不允许其他默认值、回填或索引自动进入普通字段门禁。

| 迁移 | 固定SHA-256 | 专项策略 |
| --- | --- | --- |
| `239_channel_reasoning_effort_multipliers.sql` | `66feb546785dfa385d8efd268a40276cd8ffdb0cead7026cd79e339a3b69edf1` | `reasoning-empty-239-v1` |
| `240_affiliate_ledger_operation_id.sql` | `3823bfea5f64feb58f5fcebc341c6877eaefc97834b4cd652c8e83ad08ed78da` | `affiliate-operation-240-v1` |

- 兼容旧提交固定为生产v0.1.245的`9e641699209adfe84b00a22966c51655d735fe96`。审批绑定渠道、分组、账单及联盟流水旧版读写代码摘要。
- 仅PostgreSQL 18；目标表必须为无继承的普通表，每张表含索引及TOAST总量不超过16 MiB。不用估计行数代替规模限制。未知用户触发器拒绝；唯一例外为迁移184/193已有的`groups.trg_groups_auth_cache_invalidation`，绑定完整触发器定义、启用状态及`pg_get_functiondef`的SHA-256 `f1f99ee3f64e0c9f152ee2061c294d6e6b271b21d5fbeede52b68ddf2af27dae`。239只改`model_pricing`，既有触发器在其他字段未变时直接返回，不写outbox；函数变动即拒绝。
- 每个文件独立事务，保留迁移advisory lock；所有相关表先以`ACCESS EXCLUSIVE NOWAIT`获锁，再核验条件。锁等待1秒、单语句5秒、数据库事务及Go上下文10秒。拿不到锁、超时、条件失败或字段/索引漂移，均回滚当前文件且不自动重启候选。
- 239执行前拒绝非NULL的渠道旧倍率、分组显式旧倍率或非空新倍率映射。允许旧JSON空值及空映射，不把快照中“当前没有配置”当作整个并行窗口的证明。同名新字段未登记时拒绝执行。
- 原239和三项`sub2api_bg239_*`检查约束在同一事务提交。新旧应用在共存期不能配置推理倍率；普通价格及其他设置可以继续保存。旧版NULL与新版空映射均表示不额外加价。管理端在此窗口尝试保存倍率会失败，应待发布结束后重试，不通过直接SQL绕过。
- 240保留原可空`VARCHAR(64)`和部分唯一索引。旧版明确列名写入且不写`operation_id`，NULL流水仍允许多条；新增提现操作ID维持幂等。索引仍为普通索引，会短暂持锁，不宣称零延迟。
- 切流及回滚前必须验证三项约束的真实定义、已验证状态及两个迁移摘要。失败保持当前流量；已经提交239但240失败时，旧应用继续运行且倍率保护保留。
- 自动解除仅在新提交为唯一活动槽、旧容器已移除、没有回滚操作、Compose/挂载/双入口健康通过后执行。解除同样使用NOWAIT、迁移锁及事务超时，只移除三项专用约束，不删除字段、索引、迁移账本或恢复旧数据库。
- 解除失败仍保留新实例和倍率限制，Actions报告失败；重跑同一固定发布可恢复解除。数据库已提交解除但状态写入中断时允许幂等恢复。应用普通重启不重放239，不重新冻结已开放的倍率功能。
- 回滚或候选失败保留限制，需要后续专项恢复；不得把旧版本重新指向已有新倍率的数据。原一小时观察及强制退役策略未改变，强制退役仍可能中断请求并存在用量收尾未知风险。

覆盖测试包括旧新版读写与空倍率计费、全推理级别渠道/分组成本、未知SQL或策略拒绝、显式旧倍率拒绝、超限及继承表拒绝、唯一索引、同名漂移、锁冲突回滚、保护定义漂移、切流前保护、回滚保留及退役后幂等解除。隔离测试不等于生产峰值验证，也不等于备份恢复演练。
