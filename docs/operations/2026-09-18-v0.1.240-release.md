# 2026-09-18 v0.1.240 发布与生产验收

本文时间除特别说明外均为北京时间（UTC+8）。这是用户追加发布授权后的记录，不改写此前仅本地同步、未推送部署的历史。

## 授权与固定范围

- 用户明确授权：同步未完成则继续；完成后提交代码，通过CI后合入主线，更新tag并走GitHub Actions自动蓝绿部署。
- 同步前主线：`23f34132d5e591007bf8ac8472918fec3af871e5`；上游从`bdb42e22f81fcb633ff0a060961211dd2bcb515b`完整集成至`efe9aab1e4ec89a42ba45e8dac20e882c5409a6a`，共108个提交。
- 合并代码提交：`9dd767d412f9881ef2ecf7891c99ae2823c10d21`；最终同步记录与发布候选：`ef187f2ec9cd9615a3c803dffd20fc3188e33785`。相对同步前171个文件变化，58个二开编号保留；逐提交处置、冲突适配及本地验证见两份台账的本轮同步条目。
- 候选分支7项CI与2项安全扫描全部完成且成功后，03:57将同一SHA快进推送到远端main；03:58:21推送新的附注标签`v0.1.240`。没有覆盖旧标签、强推或绕过检查。
- 标签对象`f776ad57c9c6002b656a2176f4aab32b38379564`固定指向发布候选；后续VERSION回写与本记录提交不移动标签、不再次触发部署。
- 相对生产v0.1.239提交`3fce5969a3dd6ab4275572934f5d51e4d4ee790c`，数据库迁移、ent schema、deploy目录及Release工作流无变化，满足现有无迁移蓝绿门禁。

## CI与自动发布

| 阶段 | Actions运行ID | 结果 |
| --- | --- | --- |
| 候选分支CI | `35266469278` | 成功，7项任务均未跳过 |
| 候选安全扫描 | `35266469284` | 成功，2项任务均未跳过 |
| main CI | `35267957601` | 成功，7项任务均未跳过 |
| main安全扫描 | `35267957581` | 成功，2项任务均未跳过 |
| tag CI | `35268007948` | 成功，7项任务均未跳过 |
| tag安全扫描 | `35268007916` | 成功，2项任务均未跳过 |
| 生产只读预检 | `35266536695` | 成功 |
| Release | `35268007982` | 成功，5项任务全部实际执行并通过 |

- CI覆盖shell、后端单元/集成、前端、golangci-lint、stream-race、lifecycle-race和bluegreen-protocol；安全门禁为backend-security与frontend-security。发布证据最终绑定同一SHA的tag CI和安全扫描。
- Release完成update-version、build-frontend、release、sync-version-file和deploy-production，不是跳过部署的绿色状态。正式发布于04:18:02，6个二进制/校验附件均为uploaded。附件上传曾遇GitHub临时500，由GoReleaser自动重试成功，没有改代码、重打标签或降低门禁。
- 生产部署任务04:18:13开始，05:19:52完成。只自动部署到服务器1及指定部署目录；人工SSH均用于只读验收，没有访问旧服务器或服务器2，没有手工上传覆盖Compose。
- 工作流自动回写`backend/cmd/server/VERSION`为`0.1.240`，提交`50a189519535485643a3ea804b382fdda6bf0381`仅修改版本文件；本地通过fetch和`merge --ff-only`接收。
- 本记录和两份台账追加内容另走`codex/release-v0.1.240-evidence`分支检查，通过后才快进主线；最终记录SHA与检查结果由交付总结报告，避免文档自引用。

## 切流与最终生产验收

- 04:18:38切流至green/18082，Nginx切流到双入口确认耗时1.192秒。新实例`i4d74f73b42b8449c93895010a5e8f509`，容器`865a48c6e65d7b7c617d50aad02f68c6682d3291b311983cece07c1f9b7e5fdf`。
- 固定镜像：`saviour2411/sub2api@sha256:0c56632884b1c1d8dbf6301052dea125e257cd45d4f7a3e318254e9455d18e8b`；OCI版本0.1.240，revision与已通过门禁的候选一致。
- Actions部署窗口内，API/direct健康探针各3509次，共7018次，记录异常均为0。该结论限于健康探针，不能外推为全部客户请求无中断。
- 04:19切流后和09:21:57最终只读验收均通过：API/direct两入口`/readyz`均HTTP/2 200且同一green实例；本机`/health`返回ok。最终`stable / active=green / pending=null / last_error=null`。
- 应用、PostgreSQL、Redis均running/healthy、重启计数0、OOMKilled=false。green容器身份与启动时间在切流后至最终验收保持不变；数据库和Redis容器ID与启动时间相比发布前均未变化，没有重建。
- 双Compose完全一致，SHA256为`cd01abf4f418a143a1bfca4be568b5a1afd6ebce8fe0a8301b06caf52768f3b6`；持久化config.yaml SHA256为`8f01b13b6a126e82f7a6e3d6c9eab9fa3ebe8fcaca23a01b7de49783fdf41ed4`，均与发布前相同。
- 批准的运行资源参数、HTTP upstream策略、3600秒响应头等待及5秒usage任务超时保持不变；应用只绑定`127.0.0.1:18082`。未调整连接池、worker、内存预算、Nginx容量或生产开关。
- 应用data、PostgreSQL postgres_data、Redis redis_data仍使用指定bind mount；PostgreSQL实际data_directory为`/var/lib/postgresql/data`。其父路径既有匿名卷未用作实际数据库数据目录，没有切换到命名数据卷。
- 补充只读核对：旧blue容器与blue.sock均不存在、18080无监听，state仅保留green槽；下一轮可按现有门禁复用blue/18080。

## 旧blue强制退役与风险

- 旧实例为v0.1.239，ID`i8e7cd99e9b974216aefc077984e1f0fd`，容器`0224c4908fc77035c8eae416dc590286fa2c49bbb62312ff457b05d436c1ed9b`。按既有策略从实际切流起观察3600秒，没有提前强停。
- 05:18:39进入超时强制路径。关停前可见HTTP、SSE、上游排空、用量生产/排队/执行和后台工作均为0，但仍有78个会话租约，因此不满足自然排空条件。
- 工作流执行`docker stop -t 60`；05:19:44确认退出137、OOMKilled=false。日志取证可见usage_drop_count=0、forced_shutdown_count=0，但clean_exit=false、usage_loss_unknown=true。
- 05:19:47完成归属清理和容器回收，记录删除72条旧实例归属、游标归零。78个关停前租约与清理时72条归属不是同一时刻/同一计数，不能据此推断具体客户请求或账单数量。
- 此次是超时强制退役，不是自然排空。旧请求或续接可能中断，无法证明上游最终用量与客户计费完整；不能将退出137、探针零错误或可见丢弃0描述为无损关停。未补写、重算或回滚历史账单。

## 证据与验证边界

- Actions产物`blue-green-deployment-35268007982-1`包含release.json、result.json、observation.json，绑定发布SHA、镜像digest、CI门禁、无迁移基线和本轮强制退役结果。
- 项目内`output/release-20260918-efe9aab1e`保留脱敏CI、部署产物、切流/最终审计及三分钟间隔的只读排空采样，不纳入提交。人工采样不是连续探针；连续探针数量来自本轮Actions产物。
- 没有执行真实付费Provider请求、支付/回调、完整账单核对、峰值压测或所有二开浏览器端到端。真实业务路径仍按本地同步记录列为未验证，不以健康检查代替。
- 既有依赖告警以及Go软内存与PostgreSQL缓存预算叠加的OOM风险继续保留；本次CI成功及未观测OOM不构成峰值安全证明。
