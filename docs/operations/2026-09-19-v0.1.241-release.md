# 2026-09-19 v0.1.241 发布与生产验收

本文时间除特别说明外均为北京时间（UTC+8）。用户在主线 CI 通过后追加授权更新标签，并通过 GitHub Actions 自动蓝绿部署。

## 授权与发布范围

- 发布提交：`eed20825008d86dda7828c3f6100b29e4054cbd8`，包含 `CUST-GW-014` 提前 JSON 保活及相关回归，原有首字统计保持不变。
- 新附注标签：`v0.1.241`；标签对象：`c72a05023561dec4ba7657bd29ae1b9e81e8d772`。固定指向已通过主线 CI 的发布提交，没有覆盖旧标签或强推。
- 发布前生产基线为 `v0.1.240`，提交 `ef187f2ec9cd9615a3c803dffd20fc3188e33785`。候选相对该基线没有数据库迁移、ent schema、部署目录或 Release 工作流变化。
- 本次不自动开启安全重试或提前保活，不修改生产资源、HTTP upstream 策略、3600秒响应头等待、5秒 usage 任务超时、数据挂载或蓝绿退役策略。
- 沿用实际切流后一小时排空、到期强制退役的既有策略；仍在处理的旧请求或续接可能中断，不将强制退役描述为无损关停。

## 发布前只读核验

- 16:10:21 服务器1只读预检：`stable / active=green / pending=null / last_error=null`，不存在另一轮未完成部署。
- 实际活动应用为 `sub2api-green`，仅绑定 `127.0.0.1:18082`。原 `sub2api` 与旧 blue/18080 已在前轮退役，本次按蓝绿状态检查实际活动容器和端口，没有启动旧应用。
- API/direct 双入口 `/readyz` 均为 HTTP/2 200，同一实例 `i4d74f73b42b8449c93895010a5e8f509`；本机18082 `/health` 返回 `ok`。
- 应用容器：`865a48c6e65d7b7c617d50aad02f68c6682d3291b311983cece07c1f9b7e5fdf`。PostgreSQL：`c173e2ee1480fd3c25d1627a427cc635bb3273ae0ecc3fd6a7935978548e97cb`。Redis：`7372590688e9aa0a31f554364e18365f4fea6ffba686e877661e31b18dc674b6`。三者均为 running/healthy、重启0、OOMKilled=false。
- 应用data、PostgreSQL postgres_data、Redis redis_data均保留指定bind mount；PostgreSQL父目录既有匿名卷不是实际数据目录，不切换到命名数据卷。
- 双Compose逐字节一致，SHA256为 `cd01abf4f418a143a1bfca4be568b5a1afd6ebce8fe0a8301b06caf52768f3b6`。持久化config.yaml SHA256为 `8f01b13b6a126e82f7a6e3d6c9eab9fa3ebe8fcaca23a01b7de49783fdf41ed4`，响应头超时仍为3600秒。
- `.env`中的批准资源参数与运维基线一致；PG最大连接600，预检可见客户端连接11。蓝绿实际版本以活动镜像和state为准，`.env`中遗留 `SUB2API_TAG=0.1.234` 本次不改写，也不据此重启基础Compose应用。
- 发布前安全重试持久化开关为false；提前保活尚无持久化键，按代码默认关闭。发布流程不修改这两个值；排空期间的管理端变更及最终状态见下文。

## GitHub Actions

| 阶段 | 运行ID | 结果 |
| --- | --- | --- |
| main CI | `35428933283` | 成功，7项检查均通过 |
| main安全扫描 | `35428933298` | 成功，2项检查均通过 |
| tag CI | `35431430300` | 成功，7项检查均通过 |
| tag安全扫描 | `35431430294` | 成功，2项检查均通过 |
| Release | `35431430291` | 成功，5项任务全部实际执行并通过 |

- 正式Release编号 `391997301`，16:25:44发布，6个二进制/校验附件均为uploaded。update-version、build-frontend、release、sync-version-file和deploy-production全部成功；部署任务16:25:52开始、17:27:30完成，不是跳过部署的绿色状态。
- Actions自动回写VERSION为 `0.1.241`，提交 `973ea1bacda87da373c5b57455148b197652d380` 仅修改版本文件；本地已通过fetch及 `merge --ff-only` 接收。标签继续固定指向发布候选，不跟随回写移动。

## 已确认切流

- 16:26:17切流至blue/18080，切流确认耗时1.163秒。新实例 `i78bb47c9ed29474ebecb82a36df9b1b5`，容器 `778f24f9a85d44428105ccc00d0b4db709a535c527a8b2ffbbbe1dacac0964b7`，控制面版本0.1.241、状态active。
- 固定镜像 `saviour2411/sub2api@sha256:f3b31cc205872ae0e3cabe243cda24c7764b56123a6ae6c05b5da7f95b138662`，OCI revision与发布SHA一致。
- 16:27:52只读验收：API/direct双入口均HTTP/2 200，指向同一blue实例；state为 `draining / active=blue / pending=green / last_error=null`，数据挂载与双Compose哈希保持不变。
- 16:29:10旧green只读采样仍有HTTP计数79、SSE计数56、会话租约1783；这些是不同计数，不能相加作为客户请求数。没有手工停止旧实例，持续保留到17:26:17排空窗口截止；最终强制退役结果见下文。
- 排空期间preflight读到的observation.json仍可能属于上轮发布，不能拿它证明本轮连续健康探针全部成功；最终以本轮Actions部署产物为准。

## 最终生产验收

- 17:29:02只读验收：`stable / active=blue / pending=null / last_error=null`，API/direct双入口均HTTP/2 200并指向新blue实例，本机18080健康接口返回ok。旧green容器及socket均已移除，下一轮可按既有门禁复用green槽。
- blue容器自16:26:15启动后未被替换或重启；PostgreSQL及Redis容器ID、启动时间与发布前一致，均healthy、重启0、OOMKilled=false，没有重建数据库或Redis。
- 应用仍只暴露 `127.0.0.1:18080`，三处生产bind mount、双Compose字节及哈希、config.yaml哈希、3600秒响应头等待、5秒usage超时、批准资源值与HTTP upstream策略均未变。数据库实际data_directory仍为 `/var/lib/postgresql/data`，max_connections为600。
- 最终脚本19项中18项符合发布基线；“两个开关仍关闭”的断言失败，原始退出码1和报告保留在 `.cache/release-v0.1.241-final-audit.json`，未覆盖为全通过。该差异已经通过管理审计确认，不是部署保护项被忽略。
- 两个开关现均为true，settings.updated_at均为16:37:56.02409。审计记录129155显示16:37:56.038579经admin/JWT调用 `PUT /api/v1/admin/custom-features/gateway`，请求明确设置安全重试及提前保活为true，返回200。发布执行器及本轮人工操作均未写这两个设置，不擅自回退这次管理端保存；代码默认值仍为false，历史统计未修改。

## 旧green强制退役及风险

- 17:26:18在完整一小时窗口后进入强制路径。关停前旧实例仍有HTTP计数19、SSE计数12、会话租约929；usage生产、排队、执行及上游排空计数为0。这些瞬时计数不能证明所有业务与用量已完成。
- Actions按既有策略执行60秒Docker停止宽限，17:27:23确认退出137、OOMKilled=false。可见usage_drop_count=0、forced_shutdown_count=0，但clean_exit=false、usage_loss_unknown=true。
- 17:27:27完成旧实例归属清理与回收，affinity_removed=910、游标归零。清理归属数与关停前会话租约并非同一时刻/同一计数，不能换算为实际客户请求或账单数量。
- 此次是超时强制退役，不是自然排空。旧请求或续接可能中断，用量及计费完整性无法证明；退出137、可见丢弃0和探针零异常都不能描述为无损关停。本轮没有补写、重算或回滚账单，也没有修改退役策略。

## 证据与验证边界

- 本轮Actions产物 `blue-green-deployment-35431430291-1`，产物ID `10582172736`，包含release.json、result.json和observation.json；绑定同一发布SHA、固定镜像摘要、tag CI与安全扫描，以及相对v0.1.240无迁移变化的基线。
- 本轮连续健康观察为API/direct各3508次，总计7016次，异常均为0。它只覆盖健康路径，不能外推为全部客户请求无中断，更不能抵消强制退役风险。
- 项目内 `.cache/release-v0.1.241-*` 保存发布前、切流后、约三分钟间隔排空采样、退役前后核验、脱敏管理审计及Actions证据ZIP，不纳入提交。人工采样不是连续业务监控。
- 本记录及功能台账作为独立文档提交收尾，不改已发布标签、不再触发生产部署；后续主线检查以文档提交对应的Actions记录为准。
- 未运行真实付费Provider、支付回调、完整账单核对或峰值压测；本次没有执行真实New API/Claude CLI交互验收，不以健康探针替代这些验证。
