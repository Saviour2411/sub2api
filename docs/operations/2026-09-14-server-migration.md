# 生产服务器迁移记录

关联能力：`CUST-OPS-001`、`CUST-OPS-003`、`CUST-OPS-005`。本文时间均为北京时间。

## 最终拓扑

- 新生产机：`root@216.152.153.86:22`，唯一处理业务并保存可写数据的主机。
- 旧服务器1：`root@152.53.115.95:22748`，仅保留 Nginx 入口转发，应用与数据库均已停止，容器 restart 策略为 `no`，Docker 服务和 socket 已禁用。
- 服务器2没有连接或修改。
- 应用版本保持 `0.1.232`，没有修改业务代码、构建或升级镜像，也没有进行性能调优。
- Sub2API、PostgreSQL、Redis、商店、LobeChat、文档、博客、证书及运维文件均已迁移。三个生产 bind mount 的绝对路径、双生产 Compose 的字节一致性、回环监听和实例参数保持不变。

## 切换与核验

1. 保存受限基线；导出并导入旧机五个实际运行镜像，逐个核对镜像 ID、完整环境变量、启动命令和入口程序。
2. PostgreSQL 物理基础备份通过 `pg_verifybackup`，经受限 SSH 隧道建立流复制；Redis 同样通过 SSH 复制，没有公开数据库端口。
3. 在禁止外联的独立 Docker 网络上演练恢复和同镜像应用启动，随后停止全部演练容器。演练副本不得用于生产回写。
4. 首轮于 03:04:07 开始60秒排空，仍有134条应用连接。关停未完成全部清理，容器被强制结束；日志中停止后的计费池丢弃计数至少达到31。安全检查阻止了提升与 DNS 修改，随后恢复旧业务。这个计数不能等同于已确认丢失的账单数，但不能宣称该窗口完全无损。
5. 用户明确接受短暂业务中断并要求继续完成迁移。第二轮于 03:17:47 开始转发新请求；60秒后关闭旧 Nginx 遗留连接，在旧应用网络命名空间内阻断外部 TCP 并关闭既有上游连接，保留 PostgreSQL/Redis 连接，再等待15秒让在途请求和用量收尾。
6. 03:19:05 旧 Sub2API 输出 `All cleanup steps completed`，退出码0。计费池、渠道监控、Redis、Ent 清理成功，第二轮关停日志未出现计费任务丢弃。商店与聊天容器已停止；商店从停写后的完整 SQLite/WAL 制作一致性备份，并通过 `PRAGMA quick_check`。
7. 停写后 PostgreSQL 最终位置为 `155/5311DA20`，Redis 最终复制位置为 `1364437277`，新机已追平。新旧核心数据摘要一致后停止旧数据库，才提升新库。
8. 03:19:55 新业务健康。新端开始写入后没有再次启动旧库或旧业务。
9. 先验证新 IP 直达和旧 IP 转发，再修改 Cloudflare 的10条 A 记录；DNS 改动前，新机已记录2476条有输出的流式请求和55条有输出的非流式请求。

切换前后摘要核对覆盖用户1068、账号852、API Key 1828、设置2720、支付订单1344、订阅58、测试计划69，以及用量最大 ID `25545559`。这些是停写时的快照，不是当前实时数量。物理复制的最终 WAL 位置用于核验整体已提交数据，摘要是额外抽查。

## 入口与 DNS

- 03:28:10前后，`saviour.cc.cd` 的 `@`、`www`、`ai`、`api`、`chat`、`direct`、`docs`、`py`、`shop`、`webdev` A 记录均改为 `216.152.153.86`。
- `direct` 保持灰云，其余保持橙云；TTL 均保留自动策略，Full Strict、回源端口规则以及其他 DNS/MX/TXT 记录没有改变。
- 旧 Nginx 使用固定新 IP 和原端口转发，不再解析业务域名作为上游。HTTPS 转发保留 Host/SNI、验证证书，禁用请求自动重放，保留流式响应、WebSocket、原请求体限制和3600秒代理超时。
- Nginx realip 模块仅信任旧机地址和本机代理提供的来源头；直达请求伪造 `X-Real-IP` 的检查确认，Nginx 日志仍记录实际来源地址。
- API、商店、文档分别返回200；聊天入口307为原有跳转。跨域预检204，未授权模型列表401。正式 DNS 切换后的公网健康检查通过。
- 临时 Caddy 等待代理在现有连接归零后停止；复制隧道与两个临时数据库转发防火墙规则已停用，复制账号及新库中的临时恢复配置已清理。

## 运维交接

- GitHub 生产部署目标改为新机22端口，使用独立部署密钥，不上传本机默认管理私钥。新密钥登录验证、双 Compose 核对、健康检查及12项部署安全回归通过；production 环境没有覆盖仓库 Secrets 的同名设置。Release 在新目标校验提交推送后恢复，不触发额外发布或应用重启。
- 新机每日02:00执行 `/root/vps_backup.sh`；证书续期由新机承担。旧机不再运行备份与续期。
- 备份脚本补充完整 `/etc/letsencrypt` 及续期配置，默认不按1 GiB大小排除文件；迁移暂存目录和备份临时目录不进入归档，临时工作目录限定在部署目录的 `.backup-work` 下。后续自动备份先上传并确认新归档，再执行原保留策略，防止清理失败阻断新备份上传。
- 备份打包清单测试通过；证书 `certbot renew --dry-run` 成功。首份完整本地归档于03:41:28完成，文件为 `/root/vps-backups/vps-backup-20260914-033418.tar.gz`，大小3686971343字节，SHA校验通过。在线 Redis AOF 读取变化警告已保留，不将该归档描述为跨服务全局原子快照。
- 首份 WebDAV 归档仍在后台上传，尚不能宣称完整远端备份成功。小文件上传、下载和字节比对通过；实测单连接约1.3 MB/s，上传任务 `sub2api-migration-first-upload.service` 的单次超时设为7200秒。完成后脚本核对远端大小与校验文件，才写入迁移目录的 `first-backup-upload.complete`；当前日志为 `first-upload.log`。这次手动上传不删除任何远端旧归档，日常备份计划已启用。
- 迁移私密材料位于本地 `.git/migration-20260914` 和两机部署目录下的 `.migration-20260914`，权限受限，不进入 Git。旧数据与演练副本没有自动删除。

## 退役与回退

- 最早在 2026-09-21 03:19:55 后，且连续48小时无有效业务流量，再提交旧机退役确认；不会自动关闭或删除旧机。
- 新库已经接受业务写入，不能通过启动旧库或只改 DNS 回退。优先修复新端；完整回迁需停写、反向同步、核验并重新执行单写切换。
- 应用版本、数据库连接池、按用户串行扣费、5秒用量任务超时、3600秒通用响应头超时和所有现有性能参数均未调整。
- 真实登录会话和实际支付仍需用户验收。本次没有执行真实付款，也没有利用用户凭据发起合成付费模型请求；业务验证使用已进入新机的生产流量。

## 已知关停风险

首轮暴露的串行计费关闭分支和渠道监控清理问题没有在迁移中修改：`SubmitKeyed` 的停止分支返回通用丢弃状态，而部分调用方只对专用停止状态做同步兜底；渠道监控正在执行的任务使用独立上下文，可能延长清理。相关日志已保留，历史账单没有自动补写或重算。后续修复应独立评审、测试和发布，不混入本次运维迁移。
