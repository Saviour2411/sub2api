# 2026-09-24 v0.1.246发布与生产验收记录

本文时间除特别说明外均为北京时间（UTC+8）。发布准备、实际执行与最终验收分开记录，不改写此前仅本地同步的授权边界。

## 授权与基线

- 用户批准同步上游至`a3eb7ef302961cba716dc78b39b93b60c467db0e`，随后批准推送、CI后主线合入、tag与Actions自动蓝绿部署；发现239/240门禁阻塞后，再明确批准专项兼容适配。
- 同步合并提交`970d07d2a1e67e9e848524d384c1b0b5ed07ad5c`；记录提交及已合入主线`304006b24fcc7a4b55349031955f8738f3e244d9`。
- 同步候选CI`35920884082`、安全扫描`35920884065`，主线CI`35922185867`、安全扫描`35922185846`全部通过；每轮8项CI及2项安全作业成功，未跳过。
- 生产基线v0.1.245，镜像代码`9e641699209adfe84b00a22966c51655d735fe96`，稳定blue槽，无pending。2026-09-24 05:13北京时间只读检查通过；该快照不替代打tag前再次核验。

## 专项兼容条件

方案见`blue-green-expand-migrations.md`，关联`CUST-OPS-007`。原SQL不修改，审查绑定旧代码摘要。239只允许无已配置倍率的数据；并行窗口由三项数据库约束禁止倍率变动。旧槽完全退役后才自动解除，回滚或失败保留约束。240普通唯一索引限定小表、即时表锁和事务超时。

初次只读快照为PostgreSQL18.4，相关表约24KiB/104KiB/296KiB/128KiB，显式旧倍率为0，无采样到相关锁或长事务。最近完成备份为北京时间2026-09-24 00:00至00:10，约3.76GB；没有执行恢复演练。真正执行前在事务内再次核验，数据变化可能使发布安全停止。

## 本地验证

- 专项Go单元、渠道/分组全推理级别空倍率计费、迁移239/联盟仓储回归、仓储全量隔离集成及239成功/240失败保留保护均通过。
- 55项蓝绿状态机、7项发布证据、3项预检通过；最终golangci-lint为0问题。上游原SQL、版本文件和生产Compose无改动，二开编号集合前后一致。
- 北京时间09:56再次生产只读核验通过，仍为v0.1.245稳定blue、无pending；双Compose、挂载、原资源参数、3600秒响应头及5秒usage任务均保留。尚未写入生产迁移。
- 北京时间10:00迁移复检识别groups已有184/193鉴权缓存触发器，按固定定义和函数摘要专项接纳；239更新会触发其无副作用的直接返回路径。增加该真实函数与漂移拒绝测试，未改变线上触发器。

## 发布流程与风险

- 专项分支经本地测试及固定SHA全部CI/安全门禁后快进主线，再通过新附注tag`v0.1.246`触发现有Release。不得移动已有标签。
- 不直接SSH执行生产迁移，不更改生产资源、配置或Compose；迁移由候选启动执行，切流与退役由Actions执行器控制。
- 共享库会在切流前提交新增结构；应用回退不删列、不覆盖新写入。新旧并行期间管理员配置推理倍率会被数据库拒绝，其他价格不受此限制。
- 数据库小表与超时保护不能证明零锁延迟。既有一小时观察及到期强制退役仍可能中断旧请求。历史发布曾出现退出137、非OOM、用量收尾未知，本次不得将历史记录混称为当前发布结果。
- 只有确认新活动提交/镜像、稳定无pending、退役证据、约束解除、迁移账本、双入口及原资源参数后，才能记为部署完成。本次实际结果见下文；部署成功不等于旧请求无中断或用量收尾完整。

## 固定提交、CI与发布

- 专项代码提交为`2ea8447b4b064f174013d291a4da45e9b369bde9`及`94dac3dcb880dcf96e3677827d3b4af7a55b9c98`；后者是最终发布候选。10:17:49在候选全部门禁通过后快进远端main，没有强推。
- 主线门禁通过且10:30只读复检生产基线后，10:31:55推送新附注标签`v0.1.246`，标签对象`633781b7c184824ff3f34e8f3a53dbf5b47caa09`固定指向发布候选；不覆盖旧标签。

| 阶段 | Actions运行ID | 结果 |
| --- | --- | --- |
| 最终候选CI | `35945777793` | 8项作业均成功，未跳过 |
| 最终候选安全扫描 | `35945777795` | 2项作业均成功，未跳过 |
| main CI | `35946643480` | 8项作业均成功，未跳过 |
| main安全扫描 | `35946643400` | 2项作业均成功，未跳过 |
| tag CI | `35947646975` | 8项作业均成功，未跳过 |
| tag安全扫描 | `35947646947` | 2项作业均成功，未跳过 |
| Release | `35947647075` | 10项作业及实际蓝绿部署步骤全部成功，未跳过 |

- CI覆盖shell、后端单元/集成、前端、golangci-lint、stream-race、lifecycle-race、bluegreen-protocol和release-helpers；安全扫描为backend-security与frontend-security。每轮发布证据均绑定同一完整SHA。
- 10:41:12正式发布，linux/amd64、linux/arm64、darwin/amd64、darwin/arm64、windows/amd64五个平台构建完成；5个安装包及checksums.txt共6个附件均为uploaded。
- Actions自动回写`backend/cmd/server/VERSION`为`0.1.246`，提交`5afd0fa7a32b74cf5fdc829e17578741b11f5a62`仅改变版本文件，本地通过fetch和`merge --ff-only`接收。该版本提交及后续验收记录不移动发布标签，不再次部署。
- deploy-production于10:41:22开始、11:43:05成功结束；没有重跑、降低门禁或人工执行生产迁移。人工SSH仅连接服务器1作只读检查，未访问其他服务器。

## 切流与生产验收

- 10:41:53切流至green/18082，双入口确认约1.193秒。新实例`i2cd54a9aaff444db9a906c0e88a99900`，容器`b4a61bf36c5b00108f068d350f164f6fcd685c3f8eb3596154f35f4cb9b1a8e7`；旧blue/v0.1.245进入既有3600秒排空观察期。
- 固定镜像`saviour2411/sub2api@sha256:1e699d38833e62d3b1fe427acaf7f222ac5c4f1f75b04ae69d345eca2014c093`，OCI版本0.1.246，revision与发布候选一致。
- 10:45:21切流后只读验收通过：API/direct两入口`/readyz`均HTTP/2 200且返回同一green实例，本机`/health`为ok。该采样时仍处于draining，不据此宣称退役完成。
- 11:43:01旧blue容器完成回收，11:43:02由Actions解除三项倍率保护约束。11:44:11最终只读验收确认`stable / active=green / pending=null / last_error=null`，仅保留green应用容器且无回滚操作；数据库保护约束数量为0，解除凭据绑定本次发布SHA。
- 11:44:11再次核对双入口均HTTP/2 200且指向同一green实例，本机`/health`返回ok。green身份、启动时间、重启计数及镜像摘要与10:45采样一致，未发生重建或重启。
- 批准资源参数、HTTP upstream策略、3600秒响应头等待、5秒usage任务及应用/数据库/Redis的指定bind mount不变；应用仅绑定`127.0.0.1:18082`。PostgreSQL/Redis容器ID、启动时间、重启计数、内存/连接配置与发布前逐项相同，均running/healthy且OOMKilled=false。
- 双Compose完全一致，SHA256为`cd01abf4f418a143a1bfca4be568b5a1afd6ebce8fe0a8301b06caf52768f3b6`；config.yaml SHA256为`8f01b13b6a126e82f7a6e3d6c9eab9fa3ebe8fcaca23a01b7de49783fdf41ed4`，均与发布前相同。没有覆盖生产Compose、改资源或重启数据库/Redis。
- 发布窗口内API/direct连续健康探针各3506次，共7012次，异常均为0。人工只读采样与连续探针分别留证，探针零异常不能证明所有客户请求无中断。

## 迁移验收

- 发布前309条历史迁移的文件名和摘要逐项保持不变；发布后共311条，只新增下列两项，没有改写旧账本或反向迁移。
- `239_channel_reasoning_effort_multipliers.sql`摘要`66feb546785dfa385d8efd268a40276cd8ffdb0cead7026cd79e339a3b69edf1`；两个渠道定价表的倍率列均为JSONB、非空、默认`'{}'::jsonb`。
- `240_affiliate_ledger_operation_id.sql`摘要`3823bfea5f64feb58f5fcebc341c6877eaefc97834b4cd652c8e83ad08ed78da`；operation_id为可空VARCHAR(64)、无默认值；部分唯一索引为unique/valid/ready，条件为`operation_id IS NOT NULL`。
- 三项倍率约束在新旧并行期间始终保留，旧槽回收并复核活动容器、Compose、挂载与健康后自动解除；未提前开放倍率写入，未手工改生产迁移、触发器或价格数据。

## 旧blue强制退役与风险

- 旧实例`i210ecf0343564ca082918ae611f0699d`、容器`ec766bcb0d369305d7fe6b1a8fe52636aa24283a530bdd7a2229680e5422f321`属于v0.1.245。取证的release_sha为本次`94dac3dcb880dcf96e3677827d3b4af7a55b9c98`，不是上次发布残留记录。
- 11:41:53达到首次切流后3600秒，进入既有超时强制路径。停止前HTTP=5、SSE=3、会话租约=832，其他可见工作计数为0；没有自然排空，不能称为无损退役。
- Actions执行既有60秒停止宽限，11:42:58取证为退出137、OOMKilled=false；日志读取成功，可见usage_drop_count=0、forced_shutdown_count=0，但clean_exit=false、usage_loss_unknown=true。
- 11:43:01完成旧容器回收及归属清理，删除817条旧实例归属，游标归零。832个停止前租约与817条清理记录采样时间及口径不同，不能推断具体客户请求数或账单差额。
- 旧请求或续接可能中断，不能证明上游最终用量和客户计费完整；可见用量丢弃0及健康探针零异常均不能消除此风险。本次没有改退役策略、补写或重算账单，也未用应用回退覆盖新写入数据。

## 证据与验证边界

- Actions产物`blue-green-deployment-35947647075-1`已成功上传，ID为`10788824738`，归档SHA256为`242f8248aeb016a3f8b227d916b62d465396260f44eab892a1cbe15144a05e9c`；保留绑定发布SHA/digest、迁移批准及实际退役的证据。
- 本地`output/release-20260924-v0.1.246/`保留CI/Release状态、标签与附件元数据、迁移前后完整账本、切流/排空采样、最终只读审计及逐项比对结果；不提交凭据或业务正文。
- 本记录及两份台账通过`codex/release-v0.1.246-evidence`分支提交，固定提交的CI与安全扫描成功后才快进主线；最终记录SHA和检查结果在交付总结报告，避免文档自引用。不重打标签或再次部署。
- 真实付费Provider、支付/邮件/回调、真实联盟提现、完整账单核对、备份恢复演练、全二开浏览器端到端和峰值压测仍未验证；多平台构建成功不等于各平台原生运行验收。既有Go软内存与PostgreSQL缓存预算叠加的OOM风险继续保留。
